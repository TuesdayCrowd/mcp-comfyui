# comfy-cli ground truth

> Extracted from the implementation plan when that plan was retired. Every claim
> below was executed and measured against a live ComfyUI Desktop 0.29.0 and the
> comfy-cli source, not inferred. Several were discovered only after a bug they
> caused. **Read this before changing anything that touches the CLI.**


> **Addresses, hostnames and home paths below are redacted placeholders.**
> This file is public. A remote host appears as `198.51.100.10` (RFC 5737
> TEST-NET-2, reserved for documentation and routable nowhere) and as
> `rtx-video`; home directories appear as `~/`. Only the identifying values
> were substituted — every measured behaviour, byte count, error code and
> path *shape* is exactly as observed.

Everything below was executed against a live ComfyUI Desktop (v0.29.0, `127.0.0.1:8188`) on 2026-08-02. Do not re-derive; do not assume anything not listed here.

**Environment**
- `comfy` installed via `uv tool install --python 3.13 --from <local comfy-cli tree> comfy-cli` → `~/.local/bin/comfy` (already on fish PATH via `config.fish:78`).
- Local comfy-cli tree is `v1.13.0-59-g95d7897`, newer than PyPI 1.13.0. It reports `"version": "0.0.0"` in envelopes because the tree leaves version stamping to CI. **Do not version-gate on this field.**
- Workflow files: `~/ComfyUI-Shared/user/default/workflows` (22 files, all UI/frontend format).
- ComfyUI Desktop owns the running process. It is **not** a comfy-cli workspace.

**The envelope contract** — every `comfy` command in JSON mode emits:
```json
{"schema":"envelope/1","type":"envelope","ok":true,"command":"...","version":"...",
 "where":null,"data":{...},"error":null}
```
On failure `ok:false` and `error:{code,message,hint,details}`.

**Commands this server depends on** (all verified working):

| Command | Returns |
|---|---|
| `comfy --skip-prompt workflow slots <file> --host H --port P` | `data.slots[]` — `{address,name,type,current_value,instance_id,node_type}` |
| `comfy --skip-prompt workflow set-slot <copy> ADDR=VAL... --in-place --host H --port P` | edits the file; `data.applied`, `data.warnings`, `data.wrote`. **Use in-place on a temp copy — see landmines #11/#12; `--stdout` corrupts the graph.** |
| `comfy --skip-prompt run --workflow <file> --wait --json` | NDJSON `event/1` lines, then final `envelope/1` |
| `comfy --skip-prompt discover` | Full CLI surface, schemas, error-code registry (~221KB) |
| `comfy --skip-prompt launch -- <comfyui args>` | Starts ComfyUI with passthrough startup args |

**Landmines — every one of these was verified and will silently break naive code:**

1. **`--set` cannot be combined with `--workflow`.** `cmdline.py:945` rejects it: *"--prompt/--set apply to the bundled default workflow and cannot be combined with --workflow"*. Parameterizing a workflow file is **always** two steps: `set-slot` then `run`.
2. **Piped stdout auto-selects JSON mode.** From `set-slot --help`: *"Redirecting stdout selects JSON mode."* An MCP server always pipes, so JSON is implied — but **always pass the mode flag explicitly** so behavior never depends on TTY detection.
3. **Global flags must precede the subcommand.** `--skip-prompt`, `--json-stream`, `--workspace`, `--here`, `--recent` live on the Typer root. `comfy workflow slots --skip-prompt` fails; `comfy --skip-prompt workflow slots` works.
4. **Exit code 1 is overloaded.** Missing file, bad JSON, server down, HTTP 4xx/5xx, conversion failure, validation failure, execution error — all exit 1. **Branch on `error.code` from the envelope, never on exit code.** Exit 130 = cancelled.
5. **Error codes are append-only.** Treat unknown codes as forward-compat (pass through the message), never as a parse failure.
6. **`slots` returns bare `COMBO` with no allowed values** and no min/max on numerics. Constraints come only from `/object_info`. This join is the server's core job.
7. **`--input <object_info.json>` makes `slots`/`set-slot` work offline.** Cache `/object_info` and workflow introspection no longer needs a live server.
8. **Never `comfy launch` onto an address something is already answering on.** Desktop owns 8188; a second instance there fights over VRAM and the shared model dir. Launching onto a *different, free* port is permitted by deliberate decision — the contention is real, so the caller is warned rather than blocked.
9. **`comfy launch --background` readiness is a log scrape** for the literal string `"To see the GUI go to:"`. Fragile — prefer polling `/system_stats`.
10. **`comfy launch --background` CRASHES rather than reporting a bad workspace.** With an unresolvable workspace it exits 1, writes **zero bytes to stdout**, and dumps an uncaught `FileNotFoundError` traceback to stderr — it does *not* emit the `not_in_workspace` envelope that JSON mode implies. Measured:
   ```
   $ comfy --skip-prompt --json launch --background -- --port 8189
   exit 1 | stdout: 0 bytes
   stderr: Traceback … FileNotFoundError: [Errno 2] No such file or directory:
           '~/Documents/comfy/ComfyUI'
   ```
   So a failed launch arrives as "no usable envelope", not a classified verdict. **Anything waiting for readiness must treat a non-zero exit with no envelope as terminal**, or it waits out the whole budget for a child that died in under a second.
11. **comfy-cli's own `server_already_running` is global, not per-port.** It fires from `ConfigManager().background` — comfy-cli's single-slot record of a background process *it* started — checked before the target port is even parsed (`launch.py:270`). `port_in_use` (`launch.py:321`) *is* per-port, probing `/history` on the resolved port. Consequence: an instance started by ComfyUI Desktop is invisible to `server_already_running`, but once *this server* has launched one via comfy-cli, a second launch on any other port is refused until `comfy stop` clears the slot.
12. **Host `0.0.0.0` must be rewritten to `127.0.0.1`** before use as a connect address.
13. **A ComfyUI seed cannot round-trip through JSON as an f64.** `KSampler.seed` declares `max: 18446744073709552000` — which is itself already the lossy f64 rendering of 2^64−1. Anything that reaches us via `JSON.parse` (every envelope, every slot listing) has already lost precision above 2^53. The fix is architectural and is specified in Task 3.1: **never let our JS parse or re-serialise the graph.** Copy the workflow file and let `comfy` edit the copy in place. Measured proof that the alternative fails: `set-slot 5.width=512 --stdout` on a file holding seed `18446744073709551615` returns the exact digits in the envelope, and our `JSON.parse` corrupts that untouched seed to `18446744073709552000`. Scanned all 22 of this user's workflows losslessly: zero true integer literals above 2^53 today, so this is latent rather than active — but it is silent when it fires.
14. **`comfy run --json` emits the ENTIRE workflow graph back at you, on every run.** `run/__init__.py:259` calls `renderer.event("prompt_preview", prompt=workflow)` unconditionally in stream mode — not only under `--print-prompt`. So the graph re-enters our JS through `JSON.parse` even though Task 3.1 was architected to keep it out, and every integer above 2^53 in it is rounded (measured: `18446744073709551615` → `18446744073709552000`). The submitted graph and the images are unaffected — comfy did the submit — but the **report** is wrong, and the reproduce-a-render loop reads its seed from exactly that report. Never hand a parsed `prompt` back to a caller; drop it during decode, or carry the raw line text so no JS number touches it. **This is landmine #11 recurring in a sibling module: assume it will recur again anywhere a comfy payload can contain a graph.**
15. **`slots` lists inert addresses beside effective ones, indistinguishably.** An input fed by a link **from another node in the same scope** is reported as a settable slot, but its value is resolved from that upstream node during API conversion, so anything written there is discarded. `set-slot` still reports it `applied`. Nothing anywhere signals the value was ignored.

    **"Link-fed" alone is not the test — the link's ORIGIN is.** An input fed from the subgraph *boundary* sentinel (`origin_id` < 0) is also link-fed and is **not** inert: it is the only address that works. See #26, which was found by getting this exactly backwards. `classifyInput` in `src/workflows/discover.ts` draws that distinction, and it is the whole rule.

    Measured on `audio_stable_audio_3_medium.json`. `52/6.text` (`CLIPTextEncode.text`) and `52/11.seconds` (`EmptyLatentAudio.seconds`) are link-fed and inert — setting them changed nothing, and the submitted graph carried `EmptyLatentAudio` with only `batch_size`. The **effective** controls are the upstream primitives that feed those links, and `slots` lists them too: `52/31.value` (`PrimitiveStringMultiline`), `52/36.value` (`PrimitiveFloat`), `52/3.seed` (`KSampler`).

    Setting the inert pair produced **150 seconds of tropical house** — the inner primitives' stock values — for a request of "black metal, 60 seconds". Setting the effective trio produced exactly 60.000s of the requested prompt, with `PrimitiveFloat {"value": 60}` and the seed visible in the submitted graph. **Subgraph workflows are fully usable; the hazard is address selection, not capability.** Prefer the address whose node actually holds the widget; treat any slot whose input carries a `link` and no `widget` marker as a decoy.

16. **Killing a child does NOT end a pipe read.** EOF requires every holder of the pipe's write end to close it, and spawning hands that write end to the child *and every descendant it starts*. Killing signals one pid, so a surviving grandchild pins the read open indefinitely — measured at 30s against an 800ms timeout. `comfy launch` exists precisely to leave such a descendant behind, so this is a live path, not a hypothetical.

    A timeout must therefore close **our own end of the pipe**, not merely signal the child. `src/comfy/exec.ts`'s `drain()` does this with `stream.destroy()`, which drops this process's read descriptor outright; the `orphan` fixture in `tests/exec.test.ts` pins it, asserting both that the call ends on time *and* that the descendant survives.

    Do **not** "fix" this with `detached` + a process-group kill: that would also kill the intentional long-lived ComfyUI a launch just started. Historical origin, for anyone reading old commits: this was found under Bun, where the equivalent was holding the reader and calling `reader.cancel()`, and where neither `Bun.spawn({timeout, killSignal})` nor `proc.stdout.cancel()` solved it — the latter throwing because `new Response(stream)` locks the stream. The underlying cause is POSIX file-descriptor inheritance and is not specific to any runtime.
17. **Pass `env` explicitly to every spawned child — do not rely on a runtime's default env-forwarding behaviour.** Historical origin: `Bun.spawn`, this project's spawn API before the Deno migration, did not give the child runtime mutations of `process.env`; it captured the environment as it stood at process start unless `env` was passed explicitly. Verified directly at the time: with `process.env.PROBE` set at runtime, the child saw `default=[]` but `explicit=[hello]` when spawned with `env: process.env`. This was not cosmetic — during Task 1.3 it caused a test fixture to be bypassed and the **real `comfy` binary to be invoked** by the suite. `node:child_process.spawn`, this project's spawn API now, already forwards live `process.env` by default (also verified directly), so that particular failure mode no longer reproduces — but every spawn in this project still passes `env: process.env` explicitly, on principle, so behaviour cannot regress if a runtime's default ever changes upstream.
18. **`--host`/`--port` follow the subcommand, where `--json` and `--workspace` precede it.** Verified against the installed CLI: `comfy workflow slots --help`, `comfy workflow set-slot --help`, `comfy run --help` and `comfy jobs ls|status|cancel --help` all list `--host`/`--port` as *subcommand* options, while `--json`, `--skip-prompt` and `--workspace` are Typer **root** options (landmine #3). So both orderings now appear in one argv, and a test that asserts a flag is merely *present* is not asserting the thing that breaks: `comfy --json jobs ls --host H --port P` works, `comfy jobs ls --json` does not.

    Measured live: `comfy --json jobs ls --host 198.51.100.10 --port 8189 --limit 3` returned three jobs from a remote box, and the same command against a black-holed address hung until `timeout 15` killed it — proving the flag drives a real outbound call rather than being ignored.

19. **`comfy launch` accepts no `--host` and no `--port`.** It starts a process on whichever machine runs `comfy`. Launching a remote ComfyUI is not a comfy-cli capability, so any code that "handles" a remote launch is handling something that cannot happen — see landmine #21 for what attempting it actually costs.

20. **comfy-cli does not attribute a job to a host.** A job record carries `[outputs, prompt_id, queue_position, status, updated_at, where, workflow_path, workflow_size]`. The `host` and `port` in the envelope's `data` echo the flag passed on the command line: with no flag `jobs ls` reports `127.0.0.1:8188`; with `--host 198.51.100.10` it reports that instead, over the **same 39 records**.

    Worse, asking the wrong host about a real job is not a recognisable error. Measured: `comfy --json jobs status 25e9540a-… --host 198.51.100.10 --port 8189` returned `prompt_not_found`, confidently — for a job that exists, that this server submitted to local `8188`. That is byte-identical to the answer for an id that never existed, so **a guess about which host a job is on produces a confidently wrong answer, not a detectable one.** Hence `src/jobLedger.ts`.

    `jobs cancel` fails differently again: it short-circuits on comfy-cli's own *local* records and returns `no local job with id '…'`, where `jobs status` returns `No prompt with id '…' on 198.51.100.10:8189`. Two different failures; a caller must not merge them.

21. **Pointing this server at a sleeping remote used to start a ComfyUI here.** Not a comfy-cli fact but the consequence of #19, recorded because the cost is not obvious: the launch path probed the remote, spawned `comfy launch` **locally and unconditionally**, polled the remote address for the full five-minute readiness budget, threw a timeout — and left the local process running, because `--background` had already detached it. Nothing owned the question *is this address mine to launch?* It is now asked twice: `launchInstance` refuses a non-local target, and `tools.ts` never reaches it for a remote host at all.

22. **ComfyUI's userdata API, from `app/user_manager.py` (0.30.2) and confirmed live.** This is how a remote instance's own saved workflows are reachable; `comfy` cannot enumerate another machine's.

    - `GET /api/userdata?dir=<d>&recurse=true&full_info=true` → a JSON array of `{path, size, modified, created}`. `path` is relative to the user's data root and **always `/`-separated**, because the handler does `os.path.relpath(...).replace(os.sep, '/')` — so a *Windows* host still reports `workflows/a.json`. Measured against the live Windows remote: `dir=.` returned `[{"path": "comfy.settings.json", "size": 126, "modified": 1786069244638, "created": 1786064269557}]`. A `dir` that is not there is a **404**, which for `workflows` means a fresh install rather than a fault. A missing `dir` is a 400.

      **An empty library has two shapes, not one — re-measured 2026-08-30 against the same box.** `dir=workflows` now answers **200 with `[]`** rather than 404: the directory has since been created and holds nothing. Both readings are "this host has no saved workflows", and a client must accept either, because which one it gets depends on whether anything ever wrote there — not on anything it can control. Treating only the 404 as the empty case would report an empty 200 as a library of zero *unknown* workflows, and treating only the 200 as empty would turn a fresh install into an error. The rest of the shape re-verified unchanged: `dir=.` still returns the array of `{path, size, modified, created}` (the settings file has grown 126 → 354 bytes with use), `/api/v2/userdata?path=.` answers **200** while `/api/userdata/v2` still **404**s, and a missing `dir` is still a **400**.
    - `GET /api/userdata/{file}` is **one aiohttp path segment**, so a nested path must be percent-encoded — and the handler calls `unquote` only when the name contains a `%` (`user_manager.py:94-96`), so encoding is required rather than merely tolerated.
    - That route answers with `web.FileResponse`: the file's **raw bytes**, `Content-Disposition: attachment`, `X-Content-Type-Options: nosniff`, content type from `mimetypes` (downgraded to `application/octet-stream` for anything `is_dangerous_content_type` flags). Measured: the 126-byte settings file arrived as exactly 126 bytes with four Windows `\r\n` sequences and its original indentation intact. **A workflow can therefore be fetched byte-exactly, so landmine #13's 2^64−1 seed survives the transfer** — provided nothing on this side parses it.
    - A missing file is a bare 404 with an empty body.
    - The v2 listing is `/api/v2/userdata?path=…`, **not** `/api/userdata/v2`, which 404s. This project uses v1 with `full_info=true`: it carries size and mtime in one call and is present in every build seen.

23. **A remote instance's `outputDirectory` is a path in *its* filesystem.** Measured: the live remote reports `D:\ComfyUI\output` and `--listen 198.51.100.10,127.0.0.1` (a comma-separated list — a shape `--listen` really does carry). Resolving a `/view` URL against that directory and then asking whether the file exists **here** is a category error that a Windows path only hides by accident, since it is not `isAbsolute` under POSIX. Two Unix machines sharing a layout would produce a local path naming a different image entirely. Locality must be decided on the address, not on whether the path happens to resolve.

---

*Measured 2026-08-08, against comfy-cli `v1.13.0-59-g95d7897` and a live ComfyUI **0.30.2** at `198.51.100.10:8189`.*

24. **The CLI refuses to fetch `/object_info` from a non-loopback address in local mode.** Measured, running a workflow with inputs against a remote host:

    ```
    cql_no_graph: Refusing to fetch object_info from non-loopback host
    '198.51.100.10' in local mode (potential SSRF). Use --where cloud for remote targets.
    ```

    So any command that needs node definitions for a remote — `workflow slots`, `set-slot` — must be given `--input <cached object_info>`; there is no address it will fetch from. `describe_workflow` was unaffected only because it already passed `--input`. The run path did not, and `run_workflow` with `inputs` was broken against every remote host until this was found. **The cache is the only schema source that works off-box.**

25. **A subgraph's interior nodes are renamed `outer:inner` — with a COLON — during API conversion, while `workflow slots` addresses them `outer/inner` with a SLASH.** `workflow_to_api.py:369` does `expanded["id"] = f"{outer_id}:{inner.get('id')}"`, and `run/__init__.py:213,259` echoes exactly that object as `prompt_preview`. So a caller's address `129/93.text` and the submitted graph's key `129:93` never match, and any code comparing one against the other reports **every** subgraph-interior address as absent, unconditionally — a false negative that looks exactly like a value that failed to apply. This server's `run.ts` had that bug: it reported `status: "missing"` for an address whose value had in fact been applied correctly. Chained nesting keeps chaining colons, so translation is `replaceAll("/", ":")`, not a single split.

26. **`set-slot` cannot address a subgraph instance's own widget, and says so identically for a real widget name and for garbage.** On a ComfyUI 0.30.2 workflow, `set-slot '129.text=…'` fails:

    ```
    workflow_slot_invalid: no proxyWidget mapping for 129.text; address an
    interior input directly, e.g. 129/<innerId>.<input>
    ```

    `129.unet_name`, `129.value` and the invented `129.not_a_thing` all return the byte-identical shape, so "rejected" carries no information about whether the name was real. Cause: `cql/engine.py:1710,1874` read only `instance.properties.proxyWidgets`, the **older** subgraph scheme; 0.30.2 puts the curated parameter list on the subgraph *definition* as `sg["inputs"][].linkIds`, which comfy-cli never reads. The in-source comment anticipates a *stale* `proxyWidgets`, not an *absent* one — this reads as an unhandled schema version rather than a decision.

    **The interior address is the effective one**, and the instance's own widget value is dead. Proven by a three-way `convert_ui_to_api` comparison: pristine → default text; `set-slot 129/93.text=MARKER` → `MARKER` survives conversion; mutating node 129's own `widgets_values[0]` → converted output **unchanged**. `_rewrite_internal_input` severs an interior link whose origin is the boundary sentinel `-10` and never re-attaches the outer node's widget in its place. So a boundary-fed interior input is **not** a decoy — treating it as one would refuse the only address that works.

27. **`comfy validate` breaks the envelope contract: an invalid workflow is `ok:false` with `error:null` and a fully populated `data`.** Every other command in this CLI answers `ok:false` with an `error` object, and `src/comfy/envelope.ts` treats the combination of `ok:false` and no error as a contract violation — correctly, for all of them. Measured 2026-08-09 on a graph with a bad enum and an out-of-range integer:

    ```
    exit 1 | ok:false | error:null
    data: {"valid":false,"error_count":2,"warning_count":19,"errors":[…],"warnings":[…]}
    ```

    Verified through this project's own code: `runComfy(["--json","validate",…])` on that file threw `EnvelopeParseError`, so the ordinary question "is this workflow valid?" came back as "this server and the CLI disagree about the shape of an answer". Anything wrapping `validate` must therefore use `runComfyRaw` and decode the envelope itself, treating `ok:false` **with** an error as a failure and `ok:false` **without** one as a negative answer. `src/comfy/validate.ts` does exactly that.

    Two further shapes worth knowing before designing around this command. **A workflow that validates clean still carries warnings** — `video_wan2_2_14B_i2v` reports 19, nearly all `edge_type_mismatch` from one `ComfySwitchNode` whose output type ComfyUI's catalogue cannot express — so warnings are noise to be capped, not a signal to surface whole. And **`valid: true` is structural, not semantic**: it means every node exists, every required input is present, every value is in range and every edge is wired. It does not mean the graph does what was asked. Landmine #15 is the standing proof of that difference.

    **The count is 13 on the installed build and 14 upstream, and this entry must say 14 the day the installed copy is upgraded.** Re-counted 2026-08-12 by enumerating the `"code": "<name>"` dict literals in `cql/engine.py`, which is the only file that carries them: installed gives `above_max, autogrow_bare_input, autogrow_no_slots, below_min, dangling_edge, edge_type_mismatch, non_node_key, output_index_out_of_range, prompt_no_outputs, required_input_missing, shape_mismatch, unknown_class_type, unknown_enum_value`; the checkout at `v1.15.0-1-g220f99a` adds exactly one, `no_options_available` — a COMBO field whose `object_info` declares an explicitly empty option list becomes a hard validation error rather than being silently skipped. The central claim survives the change: **all 14 appear in `error_codes.py` zero times**, on both builds, so the two vocabularies remain disjoint. Note the shape when re-running this — the diagnostics are dict literals (`"code": "x"`), not the `code="x"` keyword form the error registry uses, and grepping for the latter returns 111 codes from the wrong vocabulary.

    **Re-confirmed 2026-08-27: still 13 on the installed build, still the same 13 names, and still zero of them appear in `error_codes.py`.** The installed copy has not been upgraded, so the sentence above stands as written rather than becoming 14.

28. **A template's `output_type` is its `category_title` restated, not a property of the workflow.** Measured 2026-08-12 against the live gallery (`comfy --json --skip-prompt templates ls`, 200,675 bytes, `total_in_gallery: 578`). Grouping all 578 rows by category and taking the set of distinct `output_type` values per group gives cardinality **exactly 1 in every category**, no counterexamples:

    | `category_title` | n | `output_type` |
    |---|---:|---|
    | Video | 160 | `video` |
    | Image | 156 | `image` |
    | **Use Cases** | **103** | **`image`** |
    | **Utility** | **72** | **`image`** |
    | 3D Model | 33 | `3d` |
    | Audio | 30 | `audio` |
    | **LLM** | **17** | **`image`** |
    | **Node Basics** | **7** | **`image`** |

    Five of the eight categories — **355 of 578 templates** — collapse to `image`, so `type` is only meaningful for `video`, `audio` and `3d`. **47 of the 103 `Use Cases` rows (45.6%) produce video while typed `image`**: every one was fetched (`templates fetch <name> -o …`, 103 of 103 exit 0) and its frontend `nodes[].type` matched against `SaveVideo`, `SaveWEBM`, `SaveAnimated*`, `VHS_VideoCombine` and `CreateVideo`. Counted **by row** — these overlap heavily and are not a partition, so do not add them — `SaveVideo` appears in 38, `VHS_VideoCombine` in 12 and `CreateVideo` in 9; `SaveWEBM` and `SaveAnimated*` were in the search set and occur in none. **`--type video` recovers none of them**: `templates ls --category "Use Cases" --type video` returns `matched: 0`, which cardinality 1 already implies.

    Two limits on this number. It is a **lower bound** — static node-type inspection cannot see a third-party node saving video under a nonstandard class name — and it counts a row that carries a video node at all. Requiring at least one *active* node (`mode` not 2/mute or 4/bypass) gives **46, not 47**: `templates_rob_kling3_0_multishot_llm_product` has its single `SaveVideo` bypassed at `mode: 4`. Also, no single value serves a genuinely mixed workflow — `templates_mjm_airt_machIne` holds 11 active video nodes *and* 5 `SaveImage`. The field's cardinality is the problem, not its accuracy, so this server echoes `output_type` unchanged and lets `describe_workflow` answer the real question one call later against the real graph.

    **Re-measured 2026-08-27, at a gallery grown to 603: every structural claim above held, and only the counts moved.** The whole procedure was re-run — all 603 rows grouped by category, then all 103 `Use Cases` templates fetched again (103 of 103 exit 0) and their `nodes[].type` re-matched. Cardinality is still **exactly 1 in every category, no counterexamples**, across 8 categories; five still collapse to `image`, now **366 of 603** rather than 355 of 578. The growth landed entirely outside `Use Cases` — Video 160 → 170, Image 156 → 160, Utility 72 → 79, 3D Model 33 → 36, Audio 30 → 31, while `Use Cases` (103), LLM (17) and Node Basics (7) did not move at all.

    **The video counts reproduced exactly, template names included**: 47 rows carrying a video node, 46 with an active one, the bypassed one still `templates_rob_kling3_0_multishot_llm_product`; by row, `SaveVideo` 38, `VHS_VideoCombine` 12, `CreateVideo` 9, `SaveWEBM` and `SaveAnimated*` still zero. That is the point of re-running it. A count is a fact with a shelf life and 578 is now wrong; the *property* — that `output_type` is a category label rather than a reading of the graph — survived 25 new templates unchanged, which is better evidence for the design decision than the original single measurement was. **When this drifts again, re-measure and add a dated paragraph; do not edit the numbers above into agreement with the day.**

29. **`--tag` is exact and case-insensitive with no substring matching, over an open and partly inaccurate vocabulary.** Measured 2026-08-12: `templates ls --tag "Vid"` returns `matched: 0` while `--tag "Video"` returns 135 and `--tag "FLF2V"` returns 28 (`--tag "flf2v"` returns the same 28, so case does not matter but completeness does). The consequence is that the vocabulary must be known in advance, and it is not guessable: within `Use Cases` the video tags are `Video` (24), `Image to Video` (19), **`FLF2V` (11)**, `Video Edit` (8), `Reference to Video` (2), `Audio to Video` (1), `Text to Video` (1) and `Video to Video` (1) — and `FLF2V`, first-last-frame-to-video, carries no substring any caller would search for. Per landmine #5 the vocabulary is also open and undocumented.

    **True recall is 43 of 47 (91.5%), and the obvious arithmetic is wrong.** The tag query selects 44 `Use Cases` rows; comparing 44 against 47 gives a flattering 93.6%, but the two sets are not nested. **Take the intersection, not the sizes.** The 44 contains one row that is not video at all and misses four that are — so tags are wrong in both directions:

    - **Missed entirely (4):** `templates-led_billboard` (tags `API`, `Brand Design`; title "LED Billboard Message"; an active `SaveVideo` at node 23 — no video signal in tags, title or name), plus `template_image_speech_to_video`, `template_ltx2_3_lora_video_outpainting` and `template_purz_nb2_single_image_sprite_sheet`, which carry the word only in their *title*. The filters are per-field, so a title signal is worth nothing to a tag query.
    - **False positive (1):** `template_contact_sheet-step_2.app` is tagged `Video` and is genuinely image-only — 8 `SaveImage`, zero video nodes. It appears to describe its place in a three-step series whose *third* step produces video.

    Zero of 578 rows have an empty `tags[]` (lengths 1–5, mode 2), so the failure mode where the documented workaround has nothing to work with does not occur today. Both "silent" cases were checked against the obvious objection — their video nodes are `mode: 0`, active, not muted or bypassed. The graphs really do save video.

    **Re-measured 2026-08-27 alongside #28, and this one did not move by a single row.** The `Use Cases` tag histogram is identical — `Video` 24, `Image to Video` 19, `FLF2V` 11, `Video Edit` 8, `Reference to Video` 2, `Audio to Video` 1, `Text to Video` 1, `Video to Video` 1 — the tag query still selects 44, the intersection is still **43 of 47**, and both error directions are still the same named templates: the four missed are `templates-led_billboard`, `template_image_speech_to_video`, `template_ltx2_3_lora_video_outpainting` and `template_purz_nb2_single_image_sprite_sheet`, and the sole false positive is still `template_contact_sheet-step_2.app`. Zero of **603** rows now have an empty `tags[]`. So the vocabulary is open per landmine #5, but it is not churning: the advice to filter by `tag` and let `describe_workflow` settle it stands as measured.

30. **`comfy nodes path` does not anchor on the source type.** Re-measured 2026-08-12 against the installed build with `--input` pointed at this project's own cached `object_info`. `nodes path MODEL IMAGE` returns `count: 10`, every `from_type` an empty string, `EmptyImage` among them alongside the ByteDance/Flux/Gemini API nodes. The command *does* route in general — `CLIP → CONDITIONING` returns `CLIPTextEncode` and nine variants, each carrying a real `from_type: "CLIP"` — so the defect is narrower than "it lists nodes that output the target type". It is `can_apply`, which returns `all(t in available for t in self.required_link_types())`; `all()` over an empty list is vacuously `True`, and `required_link_types()` keeps only inputs where `p.is_link and p.required`, so any node whose required inputs are **all widgets** satisfies `can_apply` for *any* `available` set, including one sharing nothing with it. The blank `from_type` has the same cause — the representative-type loop iterates `required_link_types()` and never executes.

    Proof the source type is ignored entirely: **`LATENT → IMAGE` returns the identical 10 nodes in the identical order as `MODEL → IMAGE`** (the two envelopes differ only in the echoed `from` field). `MODEL → LATENT` shows the same shape with `Empty*Latent` nodes, none of which takes a MODEL. Separately, `IMAGE → IMAGE` returns `count: 0` from a deliberate `if from_type == to_type: return []` short-circuit, so legitimate one-hop nodes like `ImageScale` are never found either — a smaller, distinct gap.

    **Line numbers differ by build, so cite the symbol, not the offset.** On the installed copy: `required_link_types` 191, `can_apply` 206-207, the representative loop 640, the short-circuits 581 and 619. At the checkout (`v1.15.0-1-g220f99a`): 217, 232-233, 677, 618 and 656 — these are the numbers the 2026-08-11 plan document records, and they are correct for *that* tree. Upstream's own `test_exact_paths_model_to_image` (`tests/comfy_cli/cql/test_engine.py:346`) cannot catch any of it: lines 350-351 assert only `p["from"] == "MODEL"` and `p["to"] == "IMAGE"`, which are payload constants true by construction, and line 354 asserts each step's node exists. Its fixture's sole IMAGE producer is `VAEDecode`, which has real required link inputs, so the vacuous-truth case never fires. **The fix is upstream and nothing in `src/` can do it** — which is why node-by-node workflow authoring stays out of scope.

31. **`comfy workflow save|list|get|delete` accept no `--host`/`--port` at any flag position, while `slots`/`set-slot` in the same file do.** Measured 2026-08-12: `comfy --json --skip-prompt --host 127.0.0.1 --port 8188 workflow list`, the same flags after the group, and the same flags after the subcommand all exit **2** with `No such option: --host` — rejected at the root, at the group and at the subcommand alike. `--help-json` confirms it exhaustively, including flags `--help` hides: the root carries **13 params** (`--json`, `--json-stream`, `--skip-prompt`, `--where`, `--workspace`, `--here`, `--recent`, `--version`, telemetry and completion toggles) and none is host or port; the `workflow` group declares zero; and the four verbs declare only `--where`, plus `save`'s `--name`/`--description`, `list`'s `--name`/`--limit`/`--sort`/`--order`, and `get`'s `--out`/`-o`. `slots` and `set-slot` in the same file declare `--host` and `--port` outright. The address for these four therefore comes from the process-wide `COMFY_LOCAL_URL` alone.

    **The plumbing already exists.** `comfy_cli/target.py:53` declares `resolve_target(*, where, host=None, port=None, config=None)`, which accepts and threads both with precedence `flag > COMFY_LOCAL_URL > 127.0.0.1:8188`. The only obstacle is that `_resolve_where_target(where)` (installed `command/workflow.py:650`) calls `resolve_target(where=where)` and drops them on the floor. **This is why `src/comfy/userdata.ts` is not redundant with the CLI**: a named host's own saved workflows are reachable only over ComfyUI's HTTP userdata API, which that module speaks directly.

    **The write side is read-from-source, not exercised — treat it as weaker evidence than the rest of this register.** `_local_save` (installed 1038, checkout 1019) builds `_userdata_file_url(target, key, query={"overwrite": "true", "full_info": "true"})` and calls `_userdata_request(url, target, method="POST", data=text.encode("utf-8"), content_type="application/json")`; `_local_delete` (installed 1115, checkout 1096) calls the same helper with `method="DELETE"` and reads only the status. That is structurally symmetric with the `GET` calls `userdata.ts` already makes, down to the URL-building idiom — but symmetry is not a measurement, and none of it has been run against a live ComfyUI, unlike the read side which is verified against a live 0.30.2. Measure it before designing on it. Worth noting in passing: `_local_save` does `json.loads` on the workflow before posting it, so comfy-cli's own save path is subject to landmine #13.

32. **`comfy workflow notes` needs no server, no host/port and no schema source.** Measured 2026-08-17 with nothing listening anywhere (`curl 127.0.0.1:8188/system_stats` → connection refused) against the installed build at `v1.15.0-1-g220f99a`. `comfy --json --skip-prompt workflow notes <file>` returned `{"workflow": "…", "count": 2, "notes": [...]}`, exit 0, in **0.33s wall clock** over three runs (0.33 / 0.32 / 0.32) against an 84KB workflow — the floor is Python/Typer start-up, not payload, since `nodes ls` against a 1.4MB `--input` measures 0.37s. `--help-json` declares no `--host`, no `--port`, no `--where` and no `--input` on the subcommand at all: it is a local frontend-JSON parser, so unlike `slots` it needs no fourth copy of `schemaSourceArgs` and works in exactly the state this server's diagnostic tools are for. A note is seven keys — `{id, type, title, text, pos, size, subgraph}` — and `text` is the raw node's `widgets_values[0]` verbatim, with `flags`, `order`, `mode`, `properties`, `color` and `bgcolor` stripped by the CLI. **There is no `id` on the payload**, unlike `workflow slots`, which carries both `workflow` and `id`; do not assume parity between the two. Zero notes is `{count: 0, notes: []}` at exit 0 — an ordinary state, not an error, and true of two of the four real workflows tested.

    **`subgraph` is the one field whose populated shape is unmeasured.** Every note measured carries `null`, and no workflow on hand has a note inside a subgraph. It is therefore carried opaquely rather than parsed — and, as `src/workflows/notes.ts` records, `z.unknown()` alone does not do that: under zod 4 it is permissive about the value and **strict about the key**, so the field needs `.optional()` or the release that stops emitting it makes every workflow's notes unreadable.

33. **On any `comfy workflow` failure the envelope's `command` collapses from `"workflow notes"` to `"workflow"`.** Measured 2026-08-17 for `notes` and again for `slots`, so it is a behaviour of the `workflow` group rather than of one subcommand. **Never branch on `envelope.command`** — landmine #4's rule stated for a different field: branch on `error.code`, as `slots.ts` and `notes.ts` both already do. The two codes this subcommand emits are `workflow_not_found` and `workflow_not_frontend_format`, both exit 1, and **the second fires identically for an API-format workflow and for JSON that is not a workflow at all**: the CLI does not distinguish them, so neither can this server, and its own message already names the fix. Landmine #3 holds with no exception for this brand-new subcommand: `comfy workflow notes --json <file>` fails **exit 2** with "No such option: --json", while `comfy --json workflow notes <file>` works.

    A caveat worth the sentence: of four real frontend workflows tested, two carry notes, and of the four notes found, two carry model-download URLs, one is a pure VRAM/timing table and one is prose. One URL-bearing note embeds GitHub issue links alongside its model links, so a naive "first URL wins" extraction would sometimes surface a bug tracker. This server therefore extracts nothing from `text` — it carries it, capped.

34. **`comfy models` is entirely non-functional without a running server, and `models show` is cloud-only.** Measured 2026-08-17 in the same nothing-is-running state as #32. All three of `models list-folders`, `models list-folder` and `models search` return `server_not_running`; `models show` returns `models_show_local_unsupported` because it addresses cloud storage rather than the local install. So there is no offline model inventory to be had from the CLI, and the cache alternative would have to hardcode an unverifiable `ckpt_name → checkpoints/` convention that exists nowhere in the data. **`validate_workflow` already answers the question in context** — its `unknown_enum_value` diagnostics name the field and list the installed alternatives in `hint` (ground truth #27) — which is why a model-discovery tool was designed and then cut rather than built.

35. **`comfy preview` emits `envelope/1` WITHOUT `--json`.** Measured 2026-08-22 against the installed build. Every other command this server drives needs the root `--json` flag (landmine #3/#4); this one prints the envelope as its ordinary output. So a caller that reaches for `comfy --json preview` is not wrong, but a caller that omits it is not broken either — do not add the flag on the assumption that its absence explains a parse failure. `data` carries `{input, kind, preview, width, height, duration, fps, has_audio, hint}`, and `kind` is a fourth open registry in the sense of non-negotiable #2: measured values are `image`, and video/audio are documented by `--help` rather than observed here.

36. **`comfy preview`'s `width`/`height` are the SOURCE's, not the preview's.** Measured 2026-08-22: a 768×768 PNG rendered with the default `--width 480` produced a 480×480 file while the envelope reported `width: 768, height: 768`. Both numbers are true of different files, and the one a caller usually wants — how big is the thing I just made — is the one reported. Reading them as the produced preview's dimensions is wrong, and the mistake is invisible unless the two happen to differ.

37. **`comfy preview` reports `fps: 25.0` for a static PNG.** Measured 2026-08-22, alongside a correct `duration: null` and `has_audio: false` for the same file. 25.0 is ffmpeg's default frame rate, not a property of the image, so this is a decoy in the same family as ground truth #15's inert slot addresses: a field that is present, well-formed, and means nothing here. **Never surface `fps` for `kind: "image"`.** That `duration` is correctly null on the same payload makes the field look trustworthy by association, which is exactly why this is written down.

38. **`comfy preview` fails through the envelope with two codes absent from comfy-cli's published registry.** Measured 2026-08-22: a missing file gives `preview_input_not_found` ("check the path"), and a non-media file gives `preview_failed` with an ffmpeg message ("Could not probe deno.json: … Invalid data found when processing input"). Neither appears in `error_codes.py`. This is the **fourth** undocumented open-string vocabulary after `error.code`, `comfy validate`'s diagnostics (#27) and the template gallery's `output_type` (#28) — non-negotiable #2 again, and the prediction it exists to guard against has now come true three times.

39. **`comfy preview` requires ffmpeg/ffprobe, including for images.** Measured 2026-08-22 on a machine carrying both at `/opt/homebrew/bin`. The failure text on a non-media file is an ffprobe error, so the probe runs even for a still image rather than only for video and audio. An install without ffmpeg therefore gets `preview_failed` for every input, not just for video — treat preview as unavailable rather than partly working there.

40. **`comfy preview` does not reliably shrink a file; it converges.** Measured 2026-08-22, source → preview at the default 480px width: 768×768 flat synthetic PNG, 8,589 B → **31,556 B (3.67× LARGER)**; 1024×1024 random noise, 2,955,268 B → 693,995 B (0.23×); 2048×2048 random noise, 11,799,806 B → 649,734 B (0.06×). Output lands near 650–700 KB almost regardless of input, because it is a re-encode at a fixed width rather than a compression pass. So it bounds a large artifact well and **inflates a small one**, and "always preview to keep the payload small" is wrong as a general rule. Noise is the worst case for PNG, so a real render of the same dimensions is smaller than the middle two figures. Cost is ~0.3–0.55s per call.

41. **ComfyUI's `/view` DOES send `Content-Length`.** Measured 2026-08-22 against a live **0.33.3** on Windows over Tailscale: `curl -I 'http://…:8188/view?filename=Chroma-Radiance_00005_.png&subfolder=&type=output'` → `HTTP/1.1 200`, `Content-Type: image/png`, `Content-Length: 1559836`, no `Transfer-Encoding: chunked`. This matters because `fetchOutputs.ts` checks that header before writing a byte, so the auto-fetch ceiling really does decline an oversized artifact **without moving it** rather than discovering the size by downloading it. Verified end to end the same day: with the ceiling temporarily lowered to 1 MiB, that 1.49 MB image came back in `outputs.not_fetched` reading `1559836 bytes exceeds this call's 1048576-byte limit`, with `fetch_problems: []` — the byte count in the message is the remote's own header. The streaming cap remains the guarantee (the header is optional and a remote may overstate it); this measurement says the fast path is real in practice, not merely a fixture's behaviour.

42. **`comfy workflow vary` and `comfy jobs wait` exist and fit the thin-orchestrator model** — recorded for the batch/sweep work that has been deferred twice, so it need not be re-derived. Re-verified 2026-08-22 from `--help` on the installed build. `workflow vary {file}` takes repeated `--slot ADDR='<list>'`, **zips** them (so every `--slot` must carry the same length), and either returns `data.variants` in the envelope or writes `<out-dir>/<stem>_<N>.json` with `--out-dir`; it accepts `--input`, `--host` and `--port`. One landmine on it: **redirecting stdout selects JSON mode**, so `> out.ndjson` needs an explicit `--no-json`. `jobs wait [prompt_ids...]` blocks until ALL of them are terminal, with `--all` for every tracked job, `--poll-interval` defaulting to 5.0s and `--timeout` to 1800s. Both take file paths and speak the envelope, so neither needs this server to parse a graph.

43. **`comfy workflow vary` returns whole frontend graphs in `data.variants`, and `--out-dir` is what stops it.** Measured 2026-08-22 against the installed build, on the real 84 KB `video_wan2_2_14B_i2v`: without `--out-dir`, a 2-variant sweep produced an **87,453-byte envelope** whose `data.variants` is a list of complete frontend graphs — `nodes`, `links`, `last_node_id`, `groups`, `config`, `extra`, `version`, `definitions`, `revision`, `id`. Decoding that is `JSON.parse` over a graph, which is landmine #1 exactly: JavaScript rounds integers above 2^53 and a seed reaches 2^64−1, so a naive decode corrupts precisely what a seed sweep exists to vary. With `--out-dir` the graphs go to disk and `written` names the files. **This is landmine #1 in a third module, and the `--out-dir` form is the only safe one.**

44. **Under `--out-dir`, `data.variants` is present and `null` — not absent.** Measured 2026-08-22; the design that preceded this work recorded it as absent, and that was wrong. The full payload is `{workflow, count, warnings, out_dir, written, variants, stale}`. It matters only for a fixture imitating the CLI: one that dropped the key could not tell a reader of `written` from a reader of `variants`, which is the one mutant that form of decoding needs to kill. `tests/fixtures/fake-comfy` emits `"variants": null` for that reason.

45. **`vary --out-dir` preserves a 2^64−1 widget value byte-exact.** Measured 2026-08-22 with `--slot '129/86.noise_seed=[18446744073709551615,1,2]'`: `video_wan2_2_14B_i2v_000.json` contains those digits and not the rounded `18446744073709552000`. Python's arbitrary-precision `int` carries it, the same way it does for `set-slot` (landmine #11). The written files were 84,918 / 84,899 / 84,899 bytes — a size difference that is the digits themselves.

46. **`--slot` values must be JSON arrays; a comma list is refused.** Measured 2026-08-22: `--slot '129/86.noise_seed=1,2,3'` → `ok:false`, `workflow_slot_invalid`, *"--slot 129/86.noise_seed: value must be a JSON array (got str)."*, with a hint naming the correct form — `--slot '6.text=["a cat","a dog"]'`. The array is parsed with `json.loads`, so a **string element must be quoted** or the whole list is unparseable, while a **digit string must be unquoted** to arrive as an exact integer. That is the opposite of `set-slot`'s rule for the same value, where the string is the entire right-hand side; `src/workflows/vary.ts` documents the split.

47. **`vary` warns `object_info_stale` and succeeds with nothing running.** Measured 2026-08-22 with `127.0.0.1:8188` refusing: `ok:true`, `stale: true`, and one warning — *"served from cache (http://127.0.0.1:8188): cannot reach …/object_info: [Errno 61] Connection refused"* — with all three variants written. So a sweep can be **built** with no GPU up, which is the same stale-cache posture this server already has elsewhere. A sweep built against a stale cache can name a value the host no longer has, and that surfaces as a per-variant submit failure rather than a sweep failure.

48. **`comfy jobs wait` reports a partial success as a FAILURE envelope, with the answer in `error.details`.** Measured 2026-08-22 against jobs on a real host. The success arm emits the summary as `data`: `{total, completed, failed, cancelled, timed_out, elapsed_seconds, jobs:[{prompt_id, status, ok, outputs?, error_message?}]}`. Whenever **any** job failed, was cancelled, or timed out, `wait_cmd` emits `ok:false` with the *identical* summary moved into `error.details` — three codes and three exit codes:

    | outcome | code | exit |
    |---|---|---|
    | any job errored | `execution_error` | 1 |
    | any job cancelled | `cancelled` | **130** |
    | any job still running at the deadline | `wait_timeout` | 1 |

    So four completed variants and one that raised arrives as a failure, and a wrapper that lets it throw denies the caller the four that worked. This is **non-negotiable #3 one layer up**: the `ok` flag describes the batch, not whether the question was answered. `src/comfy/jobs.ts`'s `waitForJobs` reads both arms, and gates on the *code* rather than on the presence of `details` — an append-only registry means a future code may well arrive carrying a payload of its own.

49. **`wait_cmd` invents the status `timed_out`, which is in no published enum and is not terminal.** Measured 2026-08-22: waiting 2 s on an id that does not exist returned `wait_timeout` with `jobs: [{prompt_id: …, status: "timed_out", ok: false}]`. It is a statement about the *wait*, not about the job — the job is still executing inside ComfyUI — so reporting it terminal would tell a caller their render is over when it is not. Non-negotiable #2 again: `status` is an open string here exactly as it is on `jobs status`.

50. **`jobs wait` needs two budgets, and they must not be equal.** The CLI spends its `--timeout` polling and *then* builds the summary naming which jobs are still running — the only valuable part of a timeout. A caller that kills the child at the same instant replaces that answer with "the child was killed". `waitForJobs` therefore sends the caller's budget as `--timeout` (in **seconds**; the flag is a float, defaulting to 1800) and gives `runComfyRaw` 30 s more. Stating the CLI's default is also load-bearing: `runComfyRaw`'s own is 120 s, so leaving both to their defaults would SIGKILL every wait past two minutes while the CLI polled happily on.

51. **`vary`'s `stale` key is emitted only on the live-fetch path, and is ABSENT with `--input`.** Measured 2026-08-22, the same command twice against the same workflow. Pointed at `127.0.0.1:8188` with nothing listening: `data` carries `stale: true` and one `object_info_stale` warning. Pointed at a cached `object_info` with `--input <path>`: `data` has **no `stale` key at all** and `warnings: []`. This is not an edge case — it is the **normal** shape for every sweep on another machine, because comfy-cli refuses to fetch `/object_info` from a non-loopback address in local mode (`cql_no_graph`, *"potential SSRF"*, re-measured the same day against the live remote), so `--input` is the only source that works there. A schema requiring `stale` therefore fails every remote sweep with a contract violation while passing a fixture-based suite completely — which is exactly what happened here, and it was the live run that caught it. Same class as `notes.ts`'s `subgraph`: a field present in one state and absent in another.
